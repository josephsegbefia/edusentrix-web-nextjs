"use client";

import { ComingSoonPanel } from "@/components/ui/coming-soon-panel";

export default function TimetableHubPage() {
  return (
    <ComingSoonPanel
      title="Timetable hub"
      description="School-wide timetable planning is paused while we redesign it. Class, teacher, and student records are not affected."
    />
  );
}
